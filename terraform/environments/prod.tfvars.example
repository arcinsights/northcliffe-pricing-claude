# Copy to prod.tfvars and fill in your values

project_id = "your-gcp-project-id"
region     = "europe-west2"  # London region, change if needed
environment = "prod"

# Don't put secrets here - add to GitHub Secrets instead
# apify_api_token will be injected via GitHub Actions
